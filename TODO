PENDING ISSUES:
- FIX window fullscreen issue:
    DESC: when window return back from fullscreen, the window reduces in height and y position moves down with each 'F' press
- FIX window tearing issue:
    DESC: window tears when in fullscreen mode
- FIX player-camera anchor: when camera is not specified for a player, the camera must not be anchored when 'R' is pressed
- CLEAN player logic: it's a fucking mess

----------------------------------------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------------------------------------

SOLVED ISSUES:
- FIX camera pointing issue:
    DESC: whenever resizing the window, the camera points somewhere else from where the camera was looking before
- FIX: window hanging issue:
    DESC: window hangs or is 'transparent' while loading the assets
